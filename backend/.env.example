DB_HOST=localhost
DB_PORT=5432
DB_NAME=temka
DB_USER=temka
DB_PASSWORD=temka
JWT_SECRET=change-me-in-production-must-be-at-least-32-chars
DEMO_DATA_ENABLED=false

# Optional one-time bootstrap. Leave all three blank after the first admin exists.
# Password must contain 12 to 72 characters and has no built-in default.
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_NAME=
BOOTSTRAP_ADMIN_PASSWORD=
